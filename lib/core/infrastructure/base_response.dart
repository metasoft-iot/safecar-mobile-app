/// Base interface for API resources.
/// Represents a single resource in API responses.
abstract class BaseResource {
  // Marker interface for all resources
}

/// Base interface for API responses.
/// Contains the standard structure for all API responses.
abstract class BaseResponse {
  // Marker interface for all responses
}

