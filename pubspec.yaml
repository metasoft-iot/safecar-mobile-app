name: safecar_mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  go_router: ^14.1.4
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # 👉 nuevas
  dio: ^5.5.0
  json_annotation: ^4.9.0
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # para generar/actualizar los *.g.dart (si los necesitas luego)
  build_runner: ^2.4.9
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
